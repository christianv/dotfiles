## After main install

# Install Python
brew install python

# Install RVM
curl -sSL https://get.rvm.io | bash

# Install ruby 2.0.0
rvm install 2.0.0

# Install scss_lint
gem install scss_lint

# Install starred atom packages
apm stars --install
